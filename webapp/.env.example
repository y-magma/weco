# Directory holding the local WID.world dump (WID_data_<AREA>.csv files).
# Leave empty to use the default: webapp/data/WID_DATA (a symlink to the dump).
# No external API nor API key is used anymore.
WID_DATA_DIR=
