# WID.world live API (required for the running app).
# Hex string from the official R package:
#   load(system.file("R/sysdata.rda", package="wid")); paste(sprintf("%02x", api_key), collapse="")
NUXT_PUBLIC_WID_API_KEY=
NUXT_PUBLIC_WID_API_BASE_URL=https://rfap9nitz6.execute-api.eu-west-1.amazonaws.com/prod

# Golden CSV dump — used ONLY by conformance tests (API vs local reference).
# Point to the directory containing WID_data_<AREA>.csv files.
WID_REFERENCE_DATA_DIR=
